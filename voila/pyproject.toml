[build-system]
requires = [
    "setuptools>=61",
    "wheel",
    "setuptools_scm>=6.2",
]

build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "voila/rna_voila/_version.py"
root = ".."
fallback_version = "3.0.5+scmfallback"